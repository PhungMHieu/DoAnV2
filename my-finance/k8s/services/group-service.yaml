apiVersion: apps/v1
kind: Deployment
metadata:
  name: group-service
  namespace: my-finance
  labels:
    app: group-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: group-service
  template:
    metadata:
      labels:
        app: group-service
    spec:
      containers:
        - name: group-service
          image: my-finance/group-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3004
          envFrom:
            - configMapRef:
                name: my-finance-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: my-finance-secrets
                  key: DATABASE_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: my-finance-secrets
                  key: JWT_SECRET
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "300m"
          readinessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3004
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: group-service
  namespace: my-finance
spec:
  selector:
    app: group-service
  ports:
    - port: 3004
      targetPort: 3004
  type: ClusterIP
